(()=>{const API='/api/health-passport';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const icon=c=>({VACCINATION:'💉',VET:'🩺',GROOMING:'✂️',DENTAL:'🦷',MEDICATION:'💊',ALLERGY:'⚠️',OTHER:'📋'})[(c||'OTHER').toUpperCase()]||'📋';async function j(u,o){const r=await fetch(u,o);const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Request failed');return d}function addView(){if(document.getElementById('view-health'))return;const main=document.querySelector('main.content');if(!main)return;main.insertAdjacentHTML('beforeend',`<section class="view" id="view-health"><div class="health-shell"><div class="health-hero"><div><h2>🩺 Pet Health Passport</h2><p id="healthHeroText">Private care history, reminders and essential health information.</p><div class="health-private">🔒 Visible only to the logged-in pet parent.</div></div><div class="health-badge">Care lives with the pet identity</div></div><div id="healthPassportBody"></div></div></section>`)}function addNav(){const nav=document.getElementById('nav');if(!nav||document.getElementById('healthNav'))return;const btn=document.createElement('button');btn.id='healthNav';btn.dataset.view='health';btn.innerHTML='🩺 Health Passport';btn.onclick=()=>{showView('health');renderHealthPassport()};const rem=[...nav.querySelectorAll('button')].find(b=>b.dataset.view==='reminders');rem?.before(btn)}window.renderHealthPassport=async()=>{addView();const body=document.getElementById('healthPassportBody');if(!body)return;body.innerHTML='<div class="card health-card">Loading health passport...</div>';try{const d=await j(API),pet=typeof profile==='function'?profile(me):null,records=d.records||[],meds=d.medications||[],rem=(d.reminders||[]).filter(r=>!r.completed),hp=d.profile||{};document.getElementById('healthHeroText').textContent=`${pet?.name||'Your pet'}'s private care history, upcoming needs and essential health information.`;const upcoming=records.filter(r=>r.nextDueDate).sort((a,b)=>String(a.nextDueDate).localeCompare(String(b.nextDueDate)));body.innerHTML=`<div class="health-summary"><div class="health-stat"><b>${records.length}</b><span>Care records</span></div><div class="health-stat"><b>${meds.filter(m=>m.active).length}</b><span>Active medications</span></div><div class="health-stat"><b>${rem.length}</b><span>Upcoming reminders</span></div></div><div class="health-grid"><div class="card health-card"><div class="sectionhead"><h3>Essential information</h3><button class="health-soft" onclick="editHealthBasics()">Edit</button></div><div class="health-list"><div class="health-row"><div class="health-icon">⚠️</div><div><b>Allergies</b><div class="health-meta">${esc(hp.allergies||'None recorded')}</div></div></div><div class="health-row"><div class="health-icon">❤️</div><div><b>Conditions</b><div class="health-meta">${esc(hp.conditions||'None recorded')}</div></div></div><div class="health-row"><div class="health-icon">🏥</div><div><b>Primary vet</b><div class="health-meta">${esc(hp.primaryVet||'Not added')}${hp.vetPhone?' · '+esc(hp.vetPhone):''}</div></div></div><div class="health-row"><div class="health-icon">☎️</div><div><b>Emergency contact</b><div class="health-meta">${esc(hp.emergencyContact||'Not added')}${hp.emergencyPhone?' · '+esc(hp.emergencyPhone):''}</div></div></div></div></div><div class="card health-card"><div class="sectionhead"><h3>Upcoming care</h3><button class="health-add" onclick="openHealthRecord()">＋ Add record</button></div><div class="health-list">${rem.length?rem.slice(0,6).map(r=>`<div class="health-row"><div class="health-icon">${icon(r.type)}</div><div><b>${esc(r.title)}</b><div class="health-meta">${esc(r.type)} · due ${esc(r.dueDate)}</div></div></div>`).join(''):'<div class="health-empty">No upcoming care reminders.</div>'}</div></div></div><div class="health-grid"><div class="card health-card"><div class="sectionhead"><h3>Care history</h3><button class="health-add" onclick="openHealthRecord()">＋ Add</button></div><div class="health-list">${records.length?records.map(r=>`<div class="health-row"><div class="health-icon">${icon(r.category)}</div><div><b>${esc(r.title)}</b><div class="health-meta">${esc(r.category)} · ${esc(r.eventDate||'')}</div>${r.provider?`<div class="health-meta">${esc(r.provider)}</div>`:''}${r.notes?`<div class="health-meta">${esc(r.notes)}</div>`:''}${r.nextDueDate?`<div class="health-meta">Next due: ${esc(r.nextDueDate)}</div>`:''}</div><button class="health-danger" onclick="deleteHealthRecord(${r.id})">Delete</button></div>`).join(''):'<div class="health-empty">No care history yet. Add a vaccination, vet visit or grooming record.</div>'}</div></div><div class="card health-card"><div class="sectionhead"><h3>Medications</h3><button class="health-add" onclick="openMedication()">＋ Add</button></div><div class="health-list">${meds.length?meds.map(m=>`<div class="health-row"><div class="health-icon">💊</div><div><b>${esc(m.name)}</b><div class="health-meta">${esc(m.dose||'Dose not added')}${m.frequency?' · '+esc(m.frequency):''}</div><div class="health-meta">${m.startDate?'Started '+esc(m.startDate):''}${m.endDate?' · Ended '+esc(m.endDate):''}</div>${m.notes?`<div class="health-meta">${esc(m.notes)}</div>`:''}</div>${m.active?`<button class="health-soft" onclick="stopMedication(${m.id})">Mark stopped</button>`:'<span class="pill">Stopped</span>'}</div>`).join(''):'<div class="health-empty">No medications recorded.</div>'}</div></div></div>`}catch(e){body.innerHTML=`<div class="card health-card">${esc(e.message)}</div>`}};window.editHealthBasics=async()=>{let d=await j(API),hp=d.profile||{};openModal('Edit health basics',`<form class="health-form" onsubmit="saveHealthBasics(event)"><textarea id="hpAllergies" placeholder="Allergies">${esc(hp.allergies||'')}</textarea><textarea id="hpConditions" placeholder="Ongoing conditions">${esc(hp.conditions||'')}</textarea><div class="health-two"><input id="hpVet" value="${esc(hp.primaryVet||'')}" placeholder="Primary vet / clinic"><input id="hpVetPhone" value="${esc(hp.vetPhone||'')}" placeholder="Vet phone"></div><div class="health-two"><input id="hpEmergency" value="${esc(hp.emergencyContact||'')}" placeholder="Emergency contact"><input id="hpEmergencyPhone" value="${esc(hp.emergencyPhone||'')}" placeholder="Emergency phone"></div><button class="btn primary">Save health basics</button></form>`)};window.saveHealthBasics=async e=>{e.preventDefault();await j(API+'/profile',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({allergies:hpAllergies.value,conditions:hpConditions.value,primaryVet:hpVet.value,vetPhone:hpVetPhone.value,emergencyContact:hpEmergency.value,emergencyPhone:hpEmergencyPhone.value})});closeModal();toast('Health basics saved 🔒');renderHealthPassport()};window.openHealthRecord=()=>openModal('Add care record',`<form class="health-form" onsubmit="saveHealthRecord(event)"><div class="health-two"><select id="hrCategory"><option>VACCINATION</option><option>VET</option><option>GROOMING</option><option>DENTAL</option><option>ALLERGY</option><option>OTHER</option></select><input id="hrTitle" required placeholder="e.g. Rabies vaccination"></div><div class="health-two"><input id="hrDate" type="date" required><input id="hrNext" type="date" placeholder="Next due date"></div><input id="hrProvider" placeholder="Clinic / provider"><textarea id="hrNotes" placeholder="Notes"></textarea><button class="btn primary">Save care record</button></form>`);window.saveHealthRecord=async e=>{e.preventDefault();await j(API+'/records',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({category:hrCategory.value,title:hrTitle.value,eventDate:hrDate.value,nextDueDate:hrNext.value||null,provider:hrProvider.value,notes:hrNotes.value,status:'COMPLETED'})});closeModal();toast('Care record saved 🩺');renderHealthPassport()};window.deleteHealthRecord=async id=>{if(!confirm('Delete this care record?'))return;await j(API+'/records/'+id,{method:'DELETE'});toast('Care record deleted');renderHealthPassport()};window.openMedication=()=>openModal('Add medication',`<form class="health-form" onsubmit="saveMedication(event)"><input id="medName" required placeholder="Medication name"><div class="health-two"><input id="medDose" placeholder="Dose"><input id="medFreq" placeholder="Frequency, e.g. twice daily"></div><div class="health-two"><input id="medStart" type="date"><input id="medEnd" type="date"></div><textarea id="medNotes" placeholder="Instructions / notes"></textarea><button class="btn primary">Save medication</button></form>`);window.saveMedication=async e=>{e.preventDefault();await j(API+'/medications',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:medName.value,dose:medDose.value,frequency:medFreq.value,startDate:medStart.value||null,endDate:medEnd.value||null,notes:medNotes.value,active:true})});closeModal();toast('Medication added 💊');renderHealthPassport()};window.stopMedication=async id=>{await j(API+'/medications/'+id+'/stop',{method:'PATCH'});toast('Medication marked stopped');renderHealthPassport()};function boot(){addView();addNav()}setTimeout(boot,800);setTimeout(boot,2200)})();