(()=>{
const s=v=>String(v||'').trim().toLowerCase();
function terms(v){return s(v).split(/[,;/|]+/).map(x=>x.trim()).filter(Boolean)}
function overlap(a,b){const A=terms(a),B=terms(b);return A.some(x=>B.some(y=>x.includes(y)||y.includes(x)))}
function allowed(mine,other){const prefs=terms(mine.petPreferences);if(!prefs.length)return true;return prefs.some(p=>s(other.species).includes(p.replace(/s$/,''))||p.includes(s(other.species)))}
function compatibility(mine,other){let score=20,reasons=[];if(!allowed(mine,other))return {score:0,reasons:['Outside your Pet World preferences']};
 if(s(mine.species)===s(other.species)){score+=24;reasons.push('Same species')}
 if(mine.breed&&other.breed&&s(mine.breed)===s(other.breed)){score+=16;reasons.push('Same breed')}
 if(mine.location&&other.location&&s(mine.location)===s(other.location)){score+=15;reasons.push('Same area')}
 if(overlap(mine.favoriteActivities,other.favoriteActivities)){score+=12;reasons.push('Shared activities')}
 if(overlap(mine.personality,other.personality)){score+=10;reasons.push('Similar personality')}
 if(mine.age!=null&&other.age!=null){const d=Math.abs(Number(mine.age)-Number(other.age));if(d<=1){score+=8;reasons.push('Similar age')}else if(d<=3)score+=4}
 score=Math.min(99,score);if(!reasons.length)reasons.push('New pet to discover');return {score,reasons};}
window.petCompatibility=compatibility;
window.renderSmartDiscovery=function(){const grid=document.getElementById('discoverGrid');if(!grid||!window.profiles)return;const mine=profile(me),all=profiles.filter(p=>Number(p.id)!==Number(me)&&allowed(mine,p)).map(p=>({p,...compatibility(mine,p)})).sort((a,b)=>b.score-a.score);grid.innerHTML=all.length?all.map(({p,score,reasons})=>`<div class="card pet-card smart-match"><div class="match-score">${score}% match</div><div class="pet-hero" style="overflow:hidden">${p.avatarUrl?`<img src="${p.avatarUrl}" style="width:100%;height:100%;object-fit:cover">`:petEmoji(p.species)}</div><h3>${esc(p.name)} ✓</h3><div class="handle">${esc(p.handle)}</div><p>${esc(p.breed||p.species)} · ${esc(p.location||'Location private')}</p><div class="match-reasons">${reasons.slice(0,3).map(r=>`<span class="pill">${esc(r)}</span>`).join('')}</div><div class="pet-actions"><button class="btn ghost" onclick="followPet(${p.id},this)">Follow</button><button class="btn primary" onclick="friendPet(${p.id})">Friend Request</button><button class="btn ghost msg-btn" onclick="openPetChat?.(${p.id})">Message</button><button class="btn ghost" onclick="openPlayDateFor?.(${p.id})">Play Date</button></div></div>`).join(''):'<div class="card empty">No pets match your current Pet World preferences.</div>';
};
window.openPlayDateFor=function(id){const q=profile(id);if(!q)return;if(typeof openModal!=='function')return;openModal('Invite '+q.name+' to a play date',`<form class="form" onsubmit="event.preventDefault();createSmartPlayDate(${id})"><input id="smartPlayPlace" required placeholder="Pet-friendly park or venue"><input id="smartPlayTime" type="datetime-local" required><button class="btn primary">Send Play Date Request 🎾</button></form>`)};
window.createSmartPlayDate=async function(id){try{const body={hostPetId:me,guestPetId:id,location:document.getElementById('smartPlayPlace').value,scheduledAt:document.getElementById('smartPlayTime').value,status:'PENDING'};await req('/play-dates',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});closeModal();toast('Play date request sent to '+profile(id).name+' 🎾')}catch(e){toast(e.message)}};
function install(){const view=document.getElementById('view-discover');if(!view||document.getElementById('smartDiscoveryIntro'))return;const intro=document.createElement('div');intro.id='smartDiscoveryIntro';intro.className='card';intro.style.marginBottom='16px';intro.innerHTML=`<div style="display:flex;justify-content:space-between;gap:16px;align-items:center;flex-wrap:wrap"><div><b style="font-size:18px">✨ Best matches for ${esc(profile(me).name)}</b><div class="handle" style="margin-top:4px">Based on species, breed, location, age, personality, activities and your Pet World preferences.</div></div><button class="btn ghost" onclick="openPetProfileEditor?.()">Tune matching preferences</button></div>`;view.querySelector('.sectionhead')?.after(intro);setTimeout(renderSmartDiscovery,100)}
const old=window.renderDiscover;window.renderDiscover=function(list){if(list){return old?old(list):undefined}renderSmartDiscovery()};
setTimeout(install,1000);setInterval(()=>{if(document.getElementById('view-discover')?.classList.contains('active')){install()}},3000);
})();